{
  "name": "skipper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "http-server src",
    "watch-css": "cleancss --batch --batch-suffix '.min' src/css/*.css !src/css/*.min.css --watch",
    "build-css": "cleancss --batch --batch-suffix '.min' src/css/*.css !src/css/*.min.css "
  },
  "devDependencies": {
    "clean-css": "^5.3.2",
    "clean-css-cli": "^5.6.2",
    "http-server": "^14.1.1"
  }
}
